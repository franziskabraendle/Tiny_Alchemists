import empowermentexploration.utils.data_handle as data_handle


class RandModel():
    """Random model based on self-constructed game tree.
    """
    def __init__(self):
        """Initializes a little alchemy random model based on self-constructed game tree.
        """
  
                
    def get_value(self, combination):
        """Returns 1 for any given combination.

        Args:
            combination (list): List of element indices that are involved in combination.

        Returns:
            float: 1.
        """            

        return 1.0
